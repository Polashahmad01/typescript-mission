{
  //

  // String
  let myFirstName: string = 'Istiak';

  // Number
  let myNidNumber: number = 24535;

  // Boolean
  let isMarried: boolean = false;

  // Undefined
  let permanentAddress: undefined = undefined;

  // Null
  let person: null = null;

  // Array
  const stringArray: string[] = ['john', 'kate', 'nancy'];

  const rollNumberArray: number[] = [23425, 235, 3462];

  // Tuple
  const coordinatesTuple: [number, number] = [20, 30];

  const nameAgeTuple: [string, number] = ['john', 30];

  //
}